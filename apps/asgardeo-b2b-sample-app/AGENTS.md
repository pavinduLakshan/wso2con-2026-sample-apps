# Codex Collaboration Notes

- The user prefers to run project commands themselves.
- Do not start dev servers, long-running processes, or command workflows unless the user explicitly asks for them.
- When verification is useful, state the exact command the user can run instead of running it automatically.
